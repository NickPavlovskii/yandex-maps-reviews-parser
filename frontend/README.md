# Frontend

Vue 3 SPA. Запуск вместе с API: `docker compose up --build` из корня репозитория.

Локально без Docker:

```bash
npm install
npm run dev
```

Прокси `/api` → `http://localhost:8080` (см. `vite.config.ts`).
